# Scene Schema Draft

A scene records one visual teaching moment.

Suggested fields:

```yaml
id: string
visual_anchor: string
purpose: string
teacher_prompt: string
discovery: string
```

Keep it simple until real lessons prove that more structure is needed.
